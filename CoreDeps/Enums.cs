public enum TransactionType
{
    Deposit,
    Withdrawal,
    Transfer
}

public enum NotificationType
{
    Error,
    Info,
    Success
}

public enum AccountType
{
    Checkings,
    Savings,
    Locked,
    Unknown
}